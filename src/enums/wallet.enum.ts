export enum DestinationWalletEnum {
  Contract = 'Contract',
  Key = 'Key',
  Custom = 'Custom',
}

export enum Paymasters {
  Stackup = 'Stackup',
  Pimlico = 'Pimlico',
}

export enum Bundlers {
  Skandha = 'Skandha',
}
